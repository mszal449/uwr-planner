import React from 'react'
import { SemesterPlannerProps } from '@/types'

const SemesterPlanner = ( { styles } : SemesterPlannerProps ) => {
  return (
    <div className={`${styles}`}>SemesterPlanner</div>
  )
}

export default SemesterPlanner